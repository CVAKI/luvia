<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permissions for Medicine Reminder System -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Luvia">

        <!-- Splash Activity - Launch Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Main Activity - Role Selection -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Doctor Dashboard Activity -->
        <activity
            android:name=".DoctorDashboardActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Patient Dashboard Activity -->
        <activity
            android:name=".PatientDashboardActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".PatientRegistrationActivity"
            android:exported="false" />

        <!-- Patient Plans Activity - Medicine Reminders -->
        <activity
            android:name=".PatientPlansActivity"
            android:label="Medicine Plans"
            android:exported="false"
            android:parentActivityName=".PatientDashboardActivity" />

        <!-- Gemini Integration Activity - AI Voice Reminders -->
        <activity
            android:name=".GeminiIntegrationActivity"
            android:label="Medicine Reminder"
            android:theme="@style/Theme.Luvia.Translucent"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true" />

        <!-- Broadcast Receiver for Medicine Alarms -->
        <receiver
            android:name=".PillAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>